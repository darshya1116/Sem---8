Practical 2A

Parallel Bubble Sort

Commands: 

nano bubble.cpp
g++ -fopenmp bubble.cpp -o bubble
./bubble

Code

#include <iostream>
#include <omp.h>

using namespace std;

// Sequential Bubble Sort
void bubbleSortSequential(int arr[], int n) {
    for (int i = 0; i < n-1; i++) {
        for (int j = 0; j < n-i-1; j++) {
            if (arr[j] > arr[j+1]) {
                swap(arr[j], arr[j+1]);
            }
        }
    }
}

// Parallel Bubble Sort (Odd-Even Method)
void bubbleSortParallel(int arr[], int n) {
    for (int i = 0; i < n; i++) {

        // Even phase
        #pragma omp parallel for
        for (int j = 0; j < n-1; j += 2) {
            if (arr[j] > arr[j+1]) {
                swap(arr[j], arr[j+1]);
            }
        }

        // Odd phase
        #pragma omp parallel for
        for (int j = 1; j < n-1; j += 2) {
            if (arr[j] > arr[j+1]) {
                swap(arr[j], arr[j+1]);
            }
        }
    }
}

// Main Function
int main() {
    int n;

    cout << "Enter number of elements: ";
    cin >> n;

    int arr[n], arr2[n];

    cout << "Enter elements:\n";
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
        arr2[i] = arr[i]; // copy for parallel
    }

    double start, end;

    // Sequential Sort
    start = omp_get_wtime();
    bubbleSortSequential(arr, n);
    end = omp_get_wtime();

    cout << "\nSequential Sorted Array: ";
    for (int i = 0; i < n; i++) cout << arr[i] << " ";

    cout << "\nTime (Sequential): " << end - start << endl;

    // Parallel Sort
    start = omp_get_wtime();
    bubbleSortParallel(arr2, n);
    end = omp_get_wtime();

    cout << "\nParallel Sorted Array: ";
    for (int i = 0; i < n; i++) cout << arr2[i] << " ";

    cout << "\nTime (Parallel): " << end - start << endl;

    return 0;
}

OUTPUt:

Enter number of elements: 5
Enter elements:
5 3 1 4 2

Sequential Sorted Array: 1 2 3 4 5
Time (Sequential): 0.00001

Parallel Sorted Array: 1 2 3 4 5
Time (Parallel): 0.000005


Pseudocode

START

1. Input array of size n

2. For i = 1 to n:
      
      Parallel for j = 0 to n-2 step 2:
            If arr[j] > arr[j+1]
                  swap(arr[j], arr[j+1])

      Parallel for j = 1 to n-2 step 2:
            If arr[j] > arr[j+1]
                  swap(arr[j], arr[j+1])

3. Print sorted array

END



